{
  "name": "htm-backend",
  "version": "1.0.0",
  "description": "This is Hotel Management Backend",
  "scripts": {
    "dev:admin": "cd admin && npm install && npm start",
    "dev:api": "cd api && npm install && npm start",
    "dev:room": "cd rooms && npm install && npm start",
    "start": "concurrently npm:dev:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kunal",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}